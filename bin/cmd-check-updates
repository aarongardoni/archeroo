#!/bin/bash

# checkupdates does NOT require sudo or -Sy
UPDATES=$(checkupdates 2>/dev/null | wc -l)
AUR_UPDATES=$(yay -Qua 2>/dev/null | wc -l)

if [ "$UPDATES" -gt 0 ] || [ "$AUR_UPDATES" -gt 0 ]; then
  echo "There are updates available."
  exit 0
else
  exit 1
fi

