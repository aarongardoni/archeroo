#!/bin/bash
# waybar exec: emit a badge only when explicitly installed packages (repo + AUR)
# have pending updates. Printing nothing leaves the module hidden.

count=$(cmd-list-updates --count)

if [[ "${count:-0}" -gt 0 ]]; then
  echo "Updates: $count"
  exit 0
else
  exit 1
fi
