#!/bin/bash

echo "Packages to update:"
checkupdates

echo
gum confirm "Update all?"

if [ $? -eq 0 ]; then
  sudo pacman -Syu --noconfirm
  yay -Syu --noconfirm
else
    exit 0
fi

# signal waybar
pkill -RTMIN+7 waybar

cmd-show-done

exit 0
